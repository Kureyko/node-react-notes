# Node WebSocket Notes Project

Yarn Monorepo project contains `server` package for backend and `web` package for frontend.

**Start Server:**

```console
yarn server
```

**Start UI:**

```console
yarn client
```
